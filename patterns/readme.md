There is patterns for testing solvers.

Each pattern contains field and solution.

Also field can contain presetted mines, which can be loaded to Tk game.

See `matrix_io.py` for detailed description.